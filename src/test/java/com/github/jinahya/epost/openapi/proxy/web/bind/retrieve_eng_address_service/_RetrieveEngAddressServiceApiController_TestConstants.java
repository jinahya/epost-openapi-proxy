package com.github.jinahya.epost.openapi.proxy.web.bind.retrieve_eng_address_service;

import lombok.extern.slf4j.Slf4j;

@Slf4j
final class _RetrieveEngAddressServiceApiController_TestConstants {

    // -----------------------------------------------------------------------------------------------------------------
     static final int ORDER_STATES = 1;

     static final int ORDER_STATE = ORDER_STATES + 1;

    // -----------------------------------------------------------------------------------------------------------------
     static final int ORDER_CITIES = ORDER_STATE + 1;

     static final int ORDER_CITY = ORDER_CITIES + 1;

    // -----------------------------------------------------------------------------------------------------------------
     static final int ORDER_ROADS = ORDER_CITY + 1;

     static final int ORDER_ROAD = ORDER_ROADS + 1;

     static final int ORDER_ROAD_ADDRESSES = ORDER_ROAD + 1;

    // -----------------------------------------------------------------------------------------------------------------
     static final int ORDER_DISTRICTS = ORDER_CITY + 1;

     static final int ORDER_DISTRICT = ORDER_DISTRICTS + 1;

     static final int ORDER_DISTRICT_ADDRESSES = ORDER_DISTRICT + 1;
}
